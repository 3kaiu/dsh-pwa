# 2026-09-11

## P1-P3 修复批次（13 项）实现、验证并提交推送

完成待办清单中全部 13 项任务，commit `e6c45fa`，已 push 到 `origin/main`。

改动：13 文件，+261/-76。

- **P1-1** `scripts/install.sh`：新增 `stop_active_dsh()`，升级前优雅停止运行中的 dsh，避免依赖树替换时状态不一致。
- **P1-2** `.github/workflows/ci-enhanced.yml`：`clang --analyze` 恒 exit 0，补 `grep -Eq 'warning:|error:'` 断言，静态分析成为真实门禁。
- **P1-3** `scripts/profile-daemon.sh`：`pgrep -f "daemon"` → `pgrep -f "$RT_HOME/daemon"`，避免匹配无关进程。
- **P2-4** `src/daemon.c`：新增 `has_cookie()` 精确匹配 cookie 名（修复 `dsh-auth-evil` 前缀绕过）；新增 `read_request_head()` 循环 recv 至 `\r\n\r\n`（修复请求头分片导致 403 误判）。
- **P2-5** `src/daemon.c`：新增 `token_json_safe()`，校验 `[A-Za-z0-9_-]` 后才嵌入 `/health` JSON。
- **P2-6** `.github/workflows/release.yml`：lock diff 步骤改为校验 lock 非空（真实门禁）。
- **P2-7** `install.sh` + `release.yml`：修通用二进制架构检测 bug；引入 `.daemon.md5` 指纹；`DAEMON_SRC` 兼容 `src/daemon.c` 与包根 `daemon.c`。
- **P2-8** 两个 workflow：`actions/*` 固定到 commit SHA。
- **P2-9** `README.md`/`tests/`：数字漂移同步（daemon 85→117KB，bats 24→39）。
- **P2-10** `install.sh`：`umask 077`。
- **P2-11** `tests/unit/install-validation.bats`：新增 ASCII 测试名守护。
- **P3-12** `daemon.c` `build_boot()` 重构 + 空闲退出兜底；`RELEASE_NOTES.md` 重写；`cleanup-deps.sh` BEFORE/AFTER 守卫；`update-dsh.sh` 原生依赖探针；`benchmark.sh` URL 修正；`smoke-test.sh` 补空闲退出断言。

### 验证结果
- `clang -O2 -Wall -Wextra -Werror` 零告警
- `shellcheck -S warning` 全部通过
- `bash -n` 全部脚本通过
- bats `tests/unit/` **39/39 通过**
- 7 项运行时 curl 回归：新 daemon 全通过；旧 daemon 在 4 项安全检查上如期失败（证明新防护真实有效）

### 过程中发现并修复的既有 bug
1. **预编译 daemon 分支从未触发**：`file -b` 对 fat/universal 二进制逐架构输出一行，`grep -c arm64` 恒为 2，`= 1` 永不成立。改为 `[[ "$(file -b ...)" == *"$(uname -m)"* ]]`。「免本地编译」承诺此前一直是空头支票。
2. **SC2034**：`stop_active_dsh()` 中 `for i in $(seq 1 14)` 的 `i` 未使用，改为 `_`。

### 环境限制（非代码缺陷）
沙箱内 `launchctl bootstrap` 返回 I/O error (5)，无法注册 LaunchAgents。`smoke-test.sh` 已用 `[SKIP]` 条件处理。空闲退出逻辑靠代码审查验证，需在真实 macOS runner 上跑 CI 确认。

---

## 冒烟测试修复（commit `e04e88d`）

CI 首次运行 (`e6c45fa`) 中「一键安装冒烟测试」失败：
- 失败项：2b/5 端口被占时 install 应显式失败
- 根因：`install.sh` 新增的 `stop_active_dsh()` 会 curl `/health` 探测；`smoke-test.sh` 的 Python 占位服务器只 `accept()` 一次即退出，`stop_active_dsh()` 吃掉这个唯一连接后端口被提前释放，install.sh 后续端口检测通过、返回 0。
- 修复：`smoke-test.sh` 占位服务器改为 `while True: s.accept(); c.close()` 循环，持续占用端口直到测试主动 kill。
- 注：上一次 CI (`d3748f8`) 此测试是通过的（当时无 `stop_active_dsh()`），证明是本次改动引入的回归，非既有缺陷。
- 验证：`bash -n` / `shellcheck` / `clang` / bats 39/39 全部通过。

---

## CI 仍红：`auto-update-verify.sh` 用例 8 假失败 —— 根因与修复

`e04e88d` 推送后 CI 仍失败，**唯一红点**是「自动更新功能验证套件」用例 8
（dsh 运行中跳过），`✗ dsh 运行中未跳过(rc=0)` + `✗ 运行中仍修改了依赖树`。
`d3748f8`（该用例诞生那次）就已同样失败 → 不是 `e04e88d` 引入的回归，是用例本身在 CI 上从未通过。

### 根因（已在本地实证，非推测）
`tests/auto-update-verify.sh` 的假 /health 服务用 `http.server.HTTPServer`，而
`HTTPServer.server_bind()` 会在 **`bind()` 与 `listen()` 之间**调用 `socket.getfqdn(host)`
做反向解析。解析慢时 socket 停在「已绑定、未监听」状态，**macOS 对此时到达的 SYN 直接丢弃**
（实测：`bind` 后延迟 `listen`，`curl -m 2` 挂满 2.012s 返回 rc=28，而非快速 ECONNREFUSED）。
于是 `update-dsh.sh` 的 `/health`（`-m 2`）与 `/stop`（`--max-time 5`）双双超时 → 读不到
`dsh:true` → 误判「dsh 未运行」→ 照常更新 → 用例假失败。

证据链：
1. CI 用例 8 耗时 **7.9s ≈ 2s + 5s**，正是两个 curl 超时之和（`d3748f8` 那次 8.0s，复现一致）。
2. 同 job 内 `security-verification.sh` 对动态回环端口 curl 全部拿到 200/403 → 网络与 curl 无碍。
3. 同 job 内 `smoke-test.sh` 的占位监听器是**裸 socket**（bind+listen 紧邻）→ 稳定通过。
4. 本机 `getfqdn('127.0.0.1')` 仅 0.007s（mDNSResponder 已缓存）→ 本机永远复现不出来。

### 修复
- `tests/auto-update-verify.sh` 用例 8：假服务改为**裸 socket**（bind+listen 之间不夹任何解析），
  就绪判定由 `sleep 0.3 + kill -0`（只证明进程活着）改为**真连一次 curl 探测**（≤5s，失败打印
  `curl rc=`，区分 7=连不上 / 28=挂到超时）；失败路径补 update.log 尾部诊断。
- `scripts/update-dsh.sh` + `scripts/install.sh`：回环 curl 全部加 `--noproxy '*'`
  （用户环境设了 `http_proxy` 且代理不转发回环时会挂到超时 → 误判 dsh 未运行 → 在用户活跃时
  替换依赖树。本机 `env` 里就真的设了 `http_proxy`）。

### 验证
- 本地全套通过：28 通过 / 9 SKIP（修复前也是 28 通过 —— 说明该用例本机无区分度，是环境依赖型假失败）。
- **负向对照**（skill `ci-gate-hardening` Step 4 要求）：把假服务响应改成 `{"dsh":false}` 跑同一用例，
  如期 `✗ dsh 运行中未跳过` + `✗ 仍修改了依赖树` → 用例仍有区分度，非空转。
- `clang -O2 -Wall -Wextra -Werror` 零告警；`shellcheck -S warning` 零告警；`bash -n` 全通过；
  bats 39/39；`check_run_blocks.mjs` 21 个 run block 零语法错误。

### 教训（可复用）
**假 HTTP 服务一律不要用 `http.server.HTTPServer`**（反向解析夹在 bind/listen 之间，CI 上必踩）。
用裸 socket 或 `TCPServer` 子类覆写 `server_bind` 跳过 `getfqdn`。
**就绪判定不要用 `kill -0`**——它只证明进程活着，不证明端口在 listen；必须真连一次。
**超时时长本身是诊断线索**：实测耗时 ≈ 各 curl `-m` 之和，即可反推是「连接被丢弃」而非「被拒绝」。

---

## 同一提交暴露出第二个缺陷：更新期被丢弃的唤醒无人重试（CI 冒烟 3b）

修完用例 8 推送后 CI 变红，但换了一个点：**冒烟 3b「并发双 /wake 幂等」卡满 300s**
（`FAIL: 双唤醒后 dsh 未就绪`，20:24:03 → 20:29:05）。同提交 `gh run rerun` 即绿（1m33s vs 7m0s）
→ 时序型，非稳定失败。

### 根因（引导页只发一次 /wake）
- `daemon.c` 守护启动后 10s 触发后台 `update-dsh.sh`；该脚本**在 `npm view` 之前**就持
  `$RT_HOME/.install.lock`（`update-dsh.sh` 第 84 行起）。
- 持锁期间 `spawn_dsh()` 走 `update_locked()` 分支**直接放弃本轮**（设计如此：避免从半更新树拉起 dsh）。
- 但只有 `/wake` 与**页面请求**会 `request_wake()`，`/health` 不会（daemon.c:873 / 904）。
- 引导页 JS 有 `fired` 守卫：**只在首次进入时 POST 一次 `/wake`**，之后仅轮询 `/health`
  （`BOOT_PAGE`，daemon.c:571）。
- 于是那次唯一唤醒被丢弃后**无人重试** → 页面永久停在「正在唤醒…」；冒烟 3b 同样只轮询
  `/health`，遂卡满 300s 超时。命中与否取决于 `npm view` 时长与 `/wake` 时机的竞争
  （该次 CI 整体偏慢：install 23s vs 16s，`npm view` 更慢 → 窗口更宽）。
- daemon.c 原注释写「下次 /wake/页面请求自然重试」——对只轮询 /health 的客户端并不成立。

### 修复
`src/daemon.c`：被锁拒绝时置 `wake_pending`；主循环在 `poll()` **之前**自愈重试（1s 节流，
故完全无连接时也按 poll_ms 推进，不会永久卡住）；`dsh_up()/spawn_pid != 0/runtime 缺失` 时
作废；`/stop` 显式作废 pending。`spawn_dsh()` 在锁仍被持有时只读一次 pid 文件即返回且不计入
崩溃失败计数，故反复调用安全。零常驻语义不变（不 tap_use，空闲仍自退）。

### 回归测试
新增 bats 用例 `wake refused by install lock is retried after lock clears without a new wake`：
持锁 → 唯一一次 /wake → 释放锁 → **只轮询 /health** → 断言 dsh 自愈就绪。
- **fail-before**：`DSH_DAEMON_SRC=/tmp/daemon_prefix.c bats ... -f "retried after lock clears"`
  对修复前源码失败（卡满 12s 无拉起）→ 用例有区分度。
- **pass-after**：对修复后源码通过。

### 验证
clang 零告警（含 universal）；shellcheck -S warning 零告警；`bash -n` 全通过；bats **40/40**
（新增 1 项）；security-verification 33/33；auto-update-verify 28 通过/9 SKIP；
本地冒烟 3b 通过（仅 5/5 `launchctl bootstrap` 因沙箱 I/O error 5 失败，既有环境限制）。

### 教训（可复用）
1. **客户端单次请求 + 服务端「丢弃且不重试」= 永久卡死**。服务端只要可能丢弃请求，
   就必须自己负责重试，不能假设客户端会再来一次。
2. **`gh run rerun` 是区分「稳定失败」与「时序型 flake」的最低成本手段**——先跑它，再动代码。
3. 排查 CI 时**同 job 内其他测试的表现是宝贵对照**：security-verification 对回环 curl 正常、
   smoke-test 裸 socket 占位监听器正常，两条对照直接把「网络问题」排除掉。
4. **测试用的假服务/假监听器一律裸 socket**；`http.server.HTTPServer` 的反向解析坑见上节。

---

## 收尾:两条 follow-up 落地

### 1. `update-dsh.sh` 的 `npm view` 加网络超时
本脚本在 `npm view` **之前**就持 `.install.lock`，而 npm 无超时参数时 **registry 黑洞（丢包而非拒绝）
会让锁被无限期持有** → PWA 一直停在引导页（守护 `update_locked()` 期间拒绝拉起 dsh）。
- 实测（黑洞 registry `10.255.255.1:81`）：**无参数 >40s 未返回**；加
  `--fetch-timeout=20000 --fetch-retries=1` 后 **3.8s** 按预期中止。
- 取 20s × 2 次 ≈ 40s 上限：检查失败只是「保持当前版本」，下轮再试，代价可接受。
- **只给「检查」加超时，不给 `pnpm update` 加** —— 更新本身耗时长，外部打断会留下半更新树
  （外部 `timeout` 杀 pnpm 会连脚本一起杀掉，备份回滚逻辑根本跑不到）。

### 2. 引导页 `/wake` 由「单次」改为「最多每 2s 重发」
旧实现 `var fired=false` + `if(!fired){fired=true;...fetch('/wake')}` —— 单次 POST 丢包
（`.catch` 缺失，rejection 无人处理）或服务端在更新期丢弃唤醒时，页面永久卡住。
新实现 `var lastWake=0` + `if(nw-lastWake>2000){...fetch('/wake').catch(function(){})}`，
重试按钮改重置 `lastWake=0`。守护侧 `request_wake` 幂等（主进程按 `spawn_pid/dsh_up` 判定，
多个唤醒至多 spawn 一次），故 2s 重发不会产生重复实例。

**为什么服务端重试和客户端重发都要留**：`/health` 不触发 spawn，只轮询 `/health` 的客户端
（冒烟测试即如此）在被拒后永不再发；且**已安装的 PWA 可能仍提供缓存中的旧引导页**，
客户端修复对存量安装不生效。服务端重试是权威兜底。

### 新增回归测试（行为级，非字符串匹配）
`tests/unit/daemon-cases.bats` → `boot page re-fires /wake until dsh becomes ready`：
从 `GET /` 抽出 `<script>`，在 node 里用桩（`fetch`/`document`/`Date.now`/`setTimeout`）驱动
`tick()` 7 轮并统计 `/wake` 次数。
- **fail-before**：修复前源码 → `7 轮 tick 只发出 1 次 /wake`（红）
- **pass-after**：修复后 → 绿
- 附带 `healths >= 3` 断言，防 harness 自身失效导致假绿。
**关键点**：不要用「源码里有没有某个字符串」当门禁——那样抓不住「守卫又被加回来」这类回归；
必须真跑那段 JS。

### 验证
clang 零告警；shellcheck 零告警；`bash -n` 全通过；bats **41/41**（新增 1 项）；
security-verification 33/33；auto-update-verify 28 通过/9 SKIP；workflow 21 个 run block 零语法错误；
本地冒烟 1–4 全过（含 3b 与引导页端到端），仅 5/5 `launchctl bootstrap` 因沙箱限制失败。

---

## 同类缺陷清扫:回环 curl 全面加超时 + 绕代理,并把不变量固化成门禁

上一轮修完两处 CI 失败后做同类清扫,发现 `smoke-test.sh` / `benchmark.sh` /
`security-verification.sh` 里的回环 curl **一个都没带超时**——即「守护已 bind 未 listen」
时(正是本次事故的机制)会让作业一直挂到 `timeout-minutes`(30min),把真实原因藏起来。

### 实测结论(全部亲自测过,非推断)

curl 8.7.1,环境里 `http_proxy` 已设置且 `no_proxy` 未设置时,**curl 默认不豁免回环**:

| 目标状态 | 默认(走代理) | `--noproxy '*'` |
|---|---|---|
| 服务已死(端口关闭) | `%{http_code}`=**502**,rc=**0** | `000`,rc=7 |
| 已 bind 未 listen | `000`,rc=28(挂满 `--max-time`) | `000`,rc=28 |

- `curl -v` 给出决定性证据:`* Uses proxy env variable http_proxy == 'http://127.0.0.1:51837'`。
  我最初拿端口 9 试出 `502 rc=0`,差点当成环境噪声;`-v` 一看就是代理。
- **rc=0 + 502 最阴险**:只看退出码的门禁会在服务已死时报成功;比对精确码的门禁则会报出
  `502` 这个守护**永远不可能返回**的值(误导性失败)。
- 代理与目标同机时能正常转发活服务 → 本地全绿,非封闭性完全隐形。
- **超时后失败信息仍要可信**:`--max-time` 到期或连不上,`%{http_code}` 打印 **`000`(不是空串)**,
  故 `实际返回 000` 依然准确。

### `set -e` 会把「看起来受保护的赋值」变成静默早退

`set -e` 下 `X=$(curl …)` 一旦失败就终止整个脚本:后续断言不再执行,**fixture 也不会被
`daemon_stop`/cleanup 清理**。而加超时正是让这条路径可达的原因。
- 实测:无 `|| true` → rc=7,连下一行都没到;加 `|| true`(必须放在 `$( )` **里面**)→ 继续执行,`X=000`。

### 落地

- `scripts/smoke-test.sh`:12 处回环 curl 加 `--max-time 5 --noproxy '*'`;SA 段 3 处加 `--noproxy '*'`;
  4 处 `$( )` 赋值加 `|| true`。
- `tests/security-verification.sh`:5 处同款 + `|| true`。
- `scripts/benchmark.sh`:顶部 `unset http_proxy …`(脚本只压本机、不做外网访问),3 处加 `--max-time 5`。
  用 unset 而非逐处 `--noproxy '*'`:hyperfine 的命令字符串会再经 `sh -c` 解析,
  未加引号的 `*` 有被 glob 展开的风险。
- `.github/workflows/ci-enhanced.yml`:性能 job 的就绪轮询 curl 加 `--max-time 2`。
- `scripts/profile-daemon.sh` 与 benchmark.sh 提示文案里的 curl 也补超时(便于直接复制)。

### 把不变量固化成门禁

`tests/unit/install-validation.bats` 新增
`every loopback curl in CI-executed shell scripts carries a timeout`:awk 先合并反斜杠续行,
再判「非注释行含 `curl` + `127.0.0.1` 但没有 `--max-time`/`-m N`」。
- **续行合并是必须的**:否则「curl 在一行、URL 在下一行」会漏检(第一版就漏了)。
- **区分度已验证**:植入违规 → bats 退出码 1 并打印 `file:line`;还原 → 通过。
- 范围排除 bats:其 curl 都在 `daemon_wait_health`(自带 `--max-time 2`)确认监听之后,
  且守护已死是连接拒绝(快速失败)而非挂起。
- 这是「断言源码文本」的**合理例外**——超时参数的存在性无法用行为观测。

### 反例:`BATS_TEST_TIMEOUT` 在本机是静默空转

本想用 `BATS_TEST_TIMEOUT=60` 一行兜住所有 bats 用例,实测**被忽略**:设 2s,含 `sleep 30`
的用例跑了 32s 仍报 `ok`。原因是 bats 的超时逻辑要调 `/bin/ps`,而沙箱拒绝
(`Operation not permitted`)。→ **依赖外部 helper 的门禁,在该 helper 不可用时会静默退化为空转**,
与 `grep -P` / `node -e` 同族。故没加它(无法验证的门禁是负债),改为在注释里写清 bats 为何不需要。

### 验证

- clang 零告警；`shellcheck -S warning scripts/*.sh tests/*.sh tests/lib/*.sh` 零告警；
  `bash -n` 全通过；workflow **21 个 run block 零语法错误**。
- bats **42/42**（新增 1 项 lint）；`security-verification` **33/33**。
- 门禁区分度双向验证:植入违规 → 退出码 1 且打印 `file:line`；还原 → 通过；
  另用「无超时的 `http://localhost:…`」负对照确认扩容到 `localhost` 后仍能命中。
- 本地冒烟 **1–4 全过**（含 step 3 完整 token 握手,即本次改动的 12 处 curl 全被真实走过；
  3b 未挂起），仅 5/5 `launchctl bootstrap` 因沙箱 I/O error 5 失败（既有环境限制，与本次改动无关）。

### 追加:门禁自身的两处漏检（自查发现并修正）

提交 `8f04b25` 后复读自己的门禁,发现它**看不全它声称覆盖的东西**,两处都修正了:

1. **按主机过滤 → 漏掉变量 URL**。原规则要求行内含 `127.0.0.1`/`localhost`,但 `benchmark.sh`
   的调用写成 `curl -fsS "$ENDPOINT"`——字面量主机名根本不在该行,于是**漏检**。
   （也就是说:如果我没手动改那一处,门禁不会替我抓到。）
   → 改为不按主机过滤,直接判「这行是不是在调用 curl」。
2. **BWK awk 的字符类陷阱**。第一版想用 `curl[ \t]+[^-A-Za-z0-9_]` 排除散文里的
   `curl rc=…`。实测该字符类**对所有 `curl -flag` 行都不匹配**:BWK awk 把 `-A` 解析成范围,
   `-` 落进了否定类——于是最常见的那种写法全部失明,而门禁仍报「0 违规」。
   → 改为 `substr(t, RSTART+RLENGTH, 1)` 取首字符逐个判断(`h` 例外以放行裸 `http://`)。
3. **扫描范围漏掉 workflow 的 `run:` 块**。门禁只扫 `scripts/`、`tests/` 下的 shell,
   但 `ci-enhanced.yml` 里那处回环 curl **也是 CI 执行的 shell**——它是我手工补的超时,
   旧门禁覆盖不到。「手工改过的地方门禁看不见」是最该避免的缺口。
   → 范围加入 `.github/workflows/*.yml`,按纯文本扫即可(注释行以 `#` 开头会被跳过、
   反斜杠续行会被合并),不必引入 YAML 解析(本机无 PyYAML,给 bats 门禁加 node 依赖不划算)。
   用例改名以反映真实范围。验证:往 workflow 的 `run:` 块里放一处无超时 curl → 失败并打印
   `.github/workflows/zz-probe.yml:9`;移除后转绿,当前树无误报。

**方法教训(重要)**:前两次探针我用了 `curl -fsS "$ENDPOINT"` 与 `http://localhost:…`,
恰好都是坏字符类能匹配的形状,于是「通过」了、什么也没证明。**门禁必须对它声称覆盖的
每一种写法分别植入违规**,否则验证本身也是假的。最终用 10 行形状矩阵(带/不带引号、
裸 URL、变量 URL、`xargs` 前缀、散文、注释、已带超时)逐行确认命中与不命中。

### CI(结论一律用 API 核对,不看管道退出码)

- `8f04b25` → run 34530179443:`conclusion=success`,四个 job 全 `success`。
- `1b064e7` → run 34530561534:`conclusion=success`,四个 job 全 `success`。
  job 日志进一步确认门禁**在 CI 里真的执行了**(不只是本地绿):
  - `ok 42 every curl in CI-executed shell scripts carries a timeout`
  - `SMOKE OK`(CI 上 step 5/5 socket activation 也跑得过,本地因沙箱跑不了)
  - 安全验证 33/33;自动更新 27 通过/10 SKIP(多出那 1 个 SKIP 是该 job 未装 shellcheck)
  - 全日志 **0** 个 `FAIL` / `not ok` 标记

### 自查时踩到的一次假绿(方法论,值得记住)

我用 `gh run watch <id> --exit-status | grep … | head -30` 判断 CI,看到 `WATCH_EXIT=0`
就以为通过了——**那其实是 `head` 的退出码,不是 run 的**。`head` 提前退出还会 SIGPIPE 掉上游,
而 `set -o pipefail` 也救不了(它会因 SIGPIPE 报非零,错得更隐蔽)。当时那个 run 其实还是
`in_progress`。改用 `cmd > log 2>&1; echo $?` 才是真状态。
**通用教训:不要让「展示/截断」环节决定判决。** 已在 `ci-gate-hardening` 技能里记为陷阱。

### 根因模式的全仓库复核(确认不只是修了一处)

原始事故的机制是「测试假服务在 `bind()` 与 `listen()` 之间做了隐藏工作
(`http.server.HTTPServer.server_bind()` 里的 `socket.getfqdn()` 反向解析)」。
只在出问题那一处修掉是不够的,于是全仓库复核了这个不变量:

- `http.server` / `HTTPServer` / `socketserver` / `SimpleHTTPRequestHandler` /
  `getfqdn` / `gethostbyaddr` / `gethostbyname` / `http.createServer` / `flask` / `uvicorn`
  → **全仓库只剩一处命中,而且是那条警告注释本身**(`auto-update-verify.sh:356`)。
- 全仓库共 3 处假服务,全部是裸 socket,且 `bind()` 后**紧邻** `listen()`,中间无任何解析动作:
  `auto-update-verify.sh:363`、`smoke-test.sh:44`(占位监听器)、`smoke-test.sh:174`(伪 dsh)。
- `tests/unit/daemon-cases.bats` 里有 10+ 个 python 假服务,**全部裸 socket**
  (逐个抽查确认 `bind`/`listen` 相邻)。bats 目录里没有任何 `HTTPServer`。

结论:该模式已彻底清除,而非只修了 CI 报错的那一处。

**顺带**:写这条复核命令时我自己踩了技能里记录的 BSD 陷阱——
`grep '^###\|^##'` 在 BSD grep 下 `\|` 是**字面量**、匹配不到任何东西(静默无输出)。
改用 `grep -E` 或 Grep 工具即可。

### 同一类问题的上一层:job 级 timeout

「给每个等待加上界」这条不变量还有更高一层:`timeout-minutes`。
`build-and-test`(30)与 `release`(25)已声明,但 `static-analysis` / `performance` /
`binary-analysis` **没声明 → 回落到 GitHub 默认 360min**,任何一步卡住白烧 6 小时 runner。
实测三者只要 17s / 18s / 10s,故给 15 / 20 / 10(两个数量级余量)。

固化为门禁 `every workflow job declares timeout-minutes`:
- 只认 4 空格缩进的 job 级声明(step 级是 8 空格)——已用探针确认「仅 step 级带
  timeout」仍判失败,不会被蒙混过去。
- 只在 `jobs:` 之后计数,避免把 `on:` 下的 `push:`/`pull_request:` 当成 job。
- 双向验证:植入缺声明的 job → 失败并列出应覆盖的 job 名;移除后转绿。

**踩坑记录**:
- 验证 YAML 时发现本机 `ruby` 被 rbenv 指向未安装的 3.1.0,完全不可用;
  改用**系统** python3(`/opt/homebrew/opt/python@3.13/libexec/bin/python3`,带 PyYAML)
  做真实解析,比缩进启发式可靠得多(托管版 python 没有 PyYAML)。
- 又一次踩 BSD `\|` 陷阱(`grep '^jobs:\|^  [a-z-]*:$'` 静默无输出)。记性不如肌肉记忆,
  **统一改用 `grep -E`**。

## 收尾:本轮扫描全部落地并验证(553cff9 为终点)

本次同族缺陷扫描到此收束,三条提交链全部 CI 绿(均以 **API** 核对,不看管道退出码):

| commit | 内容 | run | 结论 |
|---|---|---|---|
| `72c704a` | 三个 job 补 `timeout-minutes` + 门禁 | 34532246150 | success,4/4 job |
| `553cff9` | 长期记忆更新(约定/gauntlet/陷阱 10-12) | 34532830590 | success,4/4 job |

**本地 gauntlet 终态**:bats `1..43` / 0 失败;`clang -O2 -Wall -Wextra -Werror` 零告警;
`shellcheck -S warning`(scripts + tests + tests/lib)零告警;`bash -n` 全过;
PyYAML 真实解析两个 workflow(5 个 job 全部声明 timeout);`check_run_blocks.mjs`
21 个 run block / 0 语法错误。工作树干净,`main == origin/main`。

**观察(未实施,留作可选项)**:`.github/workflows/ci-enhanced.yml` 的 `on:` 无
`paths-ignore`,因此纯文档提交(如本次 memory 更新)也会触发完整 macos-latest 流水线。
本仓库 `main` 未设 branch protection(无 required status checks),所以**不存在**
「paths-ignore 导致必需检查永不上报、PR 永久 pending」的风险;若日后启用分支保护,
再叠加 `paths-ignore` 就会踩这个经典坑。当前不值得为此改动 CI。

---

## 跨仓库:3kaiu/config 的 4 个原始 URL 复核(非本项目,仅记录连续性)

用户最初给的 4 个 URL 里只有第 4 个(dsh-pwa CI job)被处理过,补查其余三个:

| URL | 结论 |
|---|---|
| config PR #40 | 已 merged(eslint 10.9.1→10.10.0),无需动作 |
| config dependabot 页 | **0 个 open 告警**(5 fixed / 14 auto_dismissed / 11 dismissed) |
| config PR #39 | 镜像同步 PR,open 6 天,`MERGEABLE` 但其 CI **永远无法变绿** |

**#39 的缺陷(与本次 dsh-pwa 同型:门禁不可能通过)**:`mirror/sync` 上
`Script Tests` / `Config Validation` 的**每一个** `pull_request` run 都停在
`conclusion=action_required` 且 `jobs=0`(创建即终止,从未启动),自 2026-09-05 起
6 天累计 16 个,每天新增 2 个。而镜像 PR 审核清单要求「CI 全绿」→ 永远无法勾选。

**根因(已用对照组锁定)**:不是测试坏,是**触发身份**。
- `push`→main(actor `3kaiu`)→ success
- `pull_request`←`dependabot[bot]` → success
- `pull_request`←`mirror/sync`(actor `github-actions[bot]`)→ action_required

`mirror-scripts.yml` 的 push 与 `gh pr create` 都走 `${{ github.token }}`,PR 遂由
`github-actions[bot]` 创建。GitHub 官方文档(*Triggering a workflow*)原文:用
`GITHUB_TOKEN` 创建/更新的 PR,其 `pull_request`(opened/synchronize/reopened)run
会进入 **approval-required** 状态,需人工点 "Approve workflows to run"。

**处置**:已开 PR #41 —— checkout 与 PR 步骤改用 `secrets.MIRROR_TOKEN || github.token`,
未配置时 `::warning::` 显式告警并回退(不静默降级),summary 增 `CI token` 列。
需用户一次性创建 fine-grained PAT(contents:write + pull-requests:write)存为
`MIRROR_TOKEN` 才算激活;未配置前无回归。CHANGELOG 的 `### Fixed` 已补条目。

**反证**:PR #41 由 `3kaiu` 推送,其自身 CI 两个 workflow 均 **success**、
`mergeStateStatus: CLEAN` —— 同仓库同 workflow 同事件,唯一变量是 actor,诊断成立。

**顺带踩到 CRLF 陷阱(见 `git-crlf-rebase-diagnosis` 技能)**:克隆 config 后
`Mirror/iringo/iRingo.News.plugin` 立刻显示为 ` M`,diff 是纯逐行重写。
该文件 blob 是 CRLF+LF 混合、违反 `.gitattributes` 的 `* text=auto eol=lf`。
**我没有把它带进提交**——只显式 `git add` 两个目标文件,并用 `git show --stat` 核对。
这与技能里写的「status 报 clean」不同(此处报 modified),已据此修正技能:
status 干净与否只是症状,判据是 **blob 行尾与 .gitattributes 冲突**。
另:镜像 PR #39 的 diff 里该文件正是这个归一化,合并后即自然修复(与 workflow 注释一致)。

## 冒烟测试后台输出检索 + socket activation 门禁修复(17:10–17:35)

**任务**:检索后台冒烟测试输出(`scripts/smoke-test.sh`)。

**首轮结果(未改任何代码)**:步骤 1–4 全过(真实安装 node v22.22.2 + dsh 0.1.5-rc.1、
幂等重跑、端口占用显式失败、引导页/唤醒/就绪门控/token 透传、空闲自停),
**步骤 5 硬 FAIL** —— `launchctl bootstrap` → `Bootstrap failed: 5: Input/output error`,
脚本 `SMOKE_EXIT=1`。

**根因(与「已用 [SKIP] 条件处理」的既有认知不符)**:第 164 行的守卫是
`[ -d ~/Library/LaunchAgents ] && [ -w … ] && launchctl print gui/$(id -u)`,
三者在本会话**全部为真**(实测 print rc=0),但 bootstrap 被拒 rc=5。
守卫探的是「能**读** GUI 域」,操作需要的是「能**注册** job」——探针弱于操作,
于是本该走的显式 SKIP 分支永不触发,环境限制被报成产品回归。

**两命令最小复现(脱离套件)**:临时 no-op plist + `launchctl bootstrap gui/501` → rc=5;
同会话 `launchctl print gui/501` → rc=0。这一对就是证据。

**修复**:新增 `launchd_gui_usable()` 守卫 —— 用唯一 label 注册 no-op job
(`/usr/bin/true`)再立即 `bootout`,失败即返回 1;原守卫条件与 SKIP 文案一并更新
(文案补「或 bootstrap 被会话限制拒绝」)。

**验证(四点全查)**:1. 新守卫在本会话返回 1;2. 旧探针仍返回 0(证明缺口真实存在);
3. 全量重跑 **`SMOKE_EXIT=0`**,日志出现 `[SKIP] socket activation(...)` 与
`SMOKE OK`,并落 `socket-activation.skipped` 标记文件;
4. 无残留:无 `com.dshpwa.probe.*` job、无 probe plist、临时目录已删。
另跑 gauntlet:`shellcheck -S warning` 干净、全部脚本 `bash -n` 通过、**bats 43/43**。

**未提交**:改动留在工作树(`scripts/smoke-test.sh`),未 commit。

**操作坑**:长跑任务不要在 Bash 调用里 `nohup … &` 后立即返回——沙箱会随调用结束
回收进程组,pnpm 收到 SIGHUP(`Hangup: 1`)导致假失败;必须用 `run_in_background`,
并用 `bash x.sh > log 2>&1; echo $?` 取真实退出码(不要经管道,`pipestatus[1]` 拿到的是
管道中段的 tee)。

**顺手补技能**:`ci-gate-hardening` 新增陷阱「能力守卫必须探测它守卫的那个操作,
而不是它的代理」——含「守卫弱于操作会把 SKIP 变成假 FAIL」与四点复验清单。

**补充:stub 三分支验证(本机做不到的 return-0 分支)**:从真实文件抽取 `launchd_gui_usable`
(非重打副本),用桩 `launchctl` 跑三例 —— bootstrap 成功 → rc=0 且 print→bootstrap→bootout
依次调用;bootstrap rc=5 → rc=1 且不 bootout;print 失败 → rc=1 且**短路不 bootstrap**。
三例均无 probe.plist / 临时目录残留。

**已提交并推送**:`a2d5811`(fix)+`c575963`(工作日志)→ origin/main。
CI run `34585045151`(sha c575963)**conclusion=success、4/4 job 全绿**(从 API 取,不经管道)。
CI 日志实证两点:
1. 冒烟步骤真实执行,`SMOKE OK (root=/tmp/dsh-install-smoke.FPiCCU)`,端口占用/引导页透传/
   空闲自停三项 OK 均在;
2. **GitHub macOS runner 有可用 GUI launchd 会话** —— 第 5 步走的是完整
   `OK: socket activation 端到端通过(激活 → 自退 → 再激活 → 空闲自退)`,
   即新守卫的 **return-0 分支已在真实 GUI 会话上被实证**(本机无法覆盖的缺口就此闭合),
   且 CI 覆盖强度不变(仍跑完整 SA,而非降级为 SKIP)。**注意:CI 有 GUI 会话,
   与本地受限会话不同,别把本机的 SKIP 当成 CI 也会 SKIP。**

## 同类清扫:并发孤儿检查的静默跳过(18:20–18:45)

按「门禁弱于它声称覆盖的范围」这一类继续扫全仓库,命中第二处(与第 5 步同文件):

**第 3b 步唯一的断言**「并发双 `/wake` 只产生 1 个 dsh 实例(无孤儿)」在 `ps` 不可用时
只打印一行括号提示就跳过(`  (ps 不可用,跳过进程计数检查)`),而脚本结尾照样打印
`SMOKE OK`。第 5 步有 `[SKIP]` 前缀 + `socket-activation.skipped` 标记,第 3b 步两者皆无——
**同一文件内两套标准**。后果:本机 `SMOKE OK` 会被读成「全部断言都跑过」,实际孤儿断言
从未执行。这正是技能里记的「委托外部 helper 的门禁退化成 no-op」;`ps` 在本仓库已
**第二次**造成门禁空转(第一次是 `BATS_TEST_TIMEOUT`)。

**先测再改,区分两种退化**:`ps -ax -o command` 在本机 **rc=126**
("Operation not permitted")且无输出 → 正确走 else 分支,不存在 n=0 的误报。
即「探针本身是好的,只是跳过不可见」,与第 5 步「探针正常但操作被拒」是不同变体。

**修复**(commit `87bc166`):else 分支改为 `[SKIP] 并发孤儿检查(...)` + 落
`orphan-count.skipped` + CI `::notice`;收尾新增跳过项汇总,遍历
`$SMOKE_ROOT/*.skipped` 集中列出,使 `SMOKE OK` 只代表「已执行的断言全过」。

**验证**:全量重跑 `SMOKE_EXIT=0`,输出同时含两条 `[SKIP]` 与汇总
(`- orphan-count` / `- socket-activation`),两个标记文件均生成;汇总循环单测
(0/1/2 个标记)行为正确;gauntlet 全绿(shellcheck、bash -n、bats 43/43、
两 workflow YAML、21 run 块 0 语法错)。

**同类排查 `install.sh` 的 LaunchAgent 段 → 结论:无此缺陷**。它用
`launchctl print gui/UID/<label>` 做**注册后置检查**(不是域级能力代理),失败时
`warn` 且 `AGENT_OK=0` → 不自动打开浏览器、不谎报成功。**这是该模式的正确范例**,
新写注册类代码应照此:探具体对象、失败可见、不触发后续成功路径。

